export type { Locale } from './i18n';
export { locales, defaultLocale, isValidLocale, localeMetadata } from './i18n';

export type { SiteMetadata, LocalizedMetadata, MetadataGenerator } from './metadata';

export type {
    PageProps,
    LayoutProps,
    LocaleSwitcherProps,
    FeatureCardProps,
    LocalizedPageProps,
} from './common';
